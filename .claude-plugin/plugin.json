{
  "name": "topo-shadow-box",
  "description": "Generate 3D-printable topographic shadow boxes",
  "version": "1.1.0",
  "mcpServers": {
    "topo-shadow-box": {
      "command": "uv",
      "args": ["--directory", "${CLAUDE_PLUGIN_ROOT}", "run", "topo-shadow-box"]
    }
  }
}
